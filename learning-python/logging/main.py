import logging


logging.debug("Hello World!")
logging.info("That's an info message")
logging.warning("that's a warning meessage")
logging.error("That's an error message")
logging.critical("That's a critical error message")